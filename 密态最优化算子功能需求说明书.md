# 密态最优化算子功能需求说明书

## 1. 功能目标

基于 SOCI 密态计算能力实现最优化算子。

输入 `n × 3` 成本矩阵，在满足 ratio 约束的情况下，每种物料从 3 种开发方式中选择 1 种，使总成本最小。

本期固定：

```text
m = 3
```

---

## 2. 输入

### 成本矩阵

```text
costs[n][3]
```

- 每行表示一种物料；
- 三列分别表示三种开发方式的成本；
- 成本为非负整数或小数；
- 成本允许为空。

示例：

```csv
material_id,method_1,method_2,method_3
A,10.5,12.1,8.4
B,20.0,,15.2
C,,18.4,11.6
```

空值表示对应开发方式不可用。

内部处理：

```text
EMPTY → PENALTY_COST
available = false
```

`PENALTY_COST` 为系统定义的极大值，但必须保证不会超过 SOCI 明文安全范围。

每行至少需要一个有效成本。若一行全部为空，返回：

```text
NO_FEASIBLE_SOLUTION
```

### ratio_threshold

```text
ratio_threshold
```

默认：

```text
0.6
```

范围：

```text
0 <= ratio_threshold < 1
```

计算时使用定点数，避免密态浮点运算。

---

## 3. 输出

```text
OptimizationResult
```

包含：

```text
total_cost
ratio
solution[n]
```

其中：

- `total_cost`：最优方案总成本；
- `ratio`：最终 ratio；
- `solution[i] ∈ {1,2,3}`：第 i 种物料选择的开发方式。

例如：

```text
solution = [1,3,2,1]
```

---

## 4. 优化模型

定义：

```text
x(i,j) ∈ {0,1}
```

表示物料 `i` 是否选择方式 `j`。

每种物料只能选择一种方式：

```text
x(i,1) + x(i,2) + x(i,3) = 1
```

空值对应方式：

```text
available(i,j) = false
→
x(i,j) = 0
```

总成本：

```text
C_total =
Σ c(i,j) × x(i,j)
```

目标：

```text
min C_total
```

---

## 5. Ratio 约束

定义：

```text
C_12 =
选择方式1和方式2产生的成本总和

C_3 =
选择方式3产生的成本总和

C_total = C_12 + C_3
```

ratio：

```text
ratio = C_12 / C_total
```

要求：

```text
ratio_threshold <= ratio < 1
```

优化过程中不直接做除法。

设：

```text
T = ratio_threshold
```

则：

```text
ratio >= T
```

转换为：

```text
(1-T) × C_12 >= T × C_3
```

采用定点数后：

```text
(SCALE-T_int) × C_12
>=
T_int × C_3
```

最终结果确定后再计算实际 ratio。

---

## 6. 密态计算要求

成本进入优化流程后以密文计算。

主要使用：

```text
Encrypt
HomomorphicAdd
ScalarMul
SecureMul
SecureCompare
SecureDiv
```

要求：

- 总成本使用同态加法；
- 常量乘法使用标量乘；
- 成本和 ratio 条件使用安全比较；
- ratio 条件使用线性化公式，不在搜索过程中反复执行安全除法；
- 中间候选成本不直接解密；
- 只解密最终授权结果。

密钥管理沿用 SOCI SGX SDK：

```text
KeyGen / Decrypt → Enclave
Encrypt / Secure Compute → 非安全区
```

---

## 7. 求解要求

该问题属于 0-1 整数优化问题。

要求：

- 输出全局最优解；
- 不以完整 `3^n` 枚举作为生产实现；
- 优化算法与 SOCI 底层算子解耦。

可采用：

```text
Branch and Bound
Dynamic Programming
或其他可保证全局最优的离散优化算法
```

同时提供明文基准求解器，用于验证密态计算结果。

---

## 8. API

核心接口：

```text
optimize(
    costs,
    ratio_threshold = 0.6
) -> OptimizationResult
```

Python 示例：

```python
result = optimizer.optimize(
    costs=[
        [10.5, 12.1, 8.4],
        [20.0, None, 15.2],
        [None, 18.4, 11.6],
    ],
    ratio_threshold=0.6
)

result.total_cost
result.ratio
result.solution
```

CSV 接口：

```text
optimize_csv(path, ratio_threshold)
```

CSV 解析属于适配层，核心算法使用统一 `CostMatrix` 数据结构。

---

## 9. 异常处理

### 非法输入

返回：

```text
INVALID_ARGUMENT
```

包括：

- `n = 0`；
- 列数不是 3；
- 成本小于 0；
- 非法数值；
- ratio_threshold 超出范围。

### 无可行解

返回：

```text
NO_FEASIBLE_SOLUTION
```

包括：

- 某物料三种方式全部为空；
- 不存在满足 ratio 约束的组合。

### 数值越界

如果定点编码或中间运算可能超过 SOCI 明文安全范围，返回：

```text
NUMERIC_RANGE_EXCEEDED
```

---

## 10. 多最优解

存在多个相同最低成本方案时：

1. 优先总成本最低；
2. 成本相同时，ratio 更接近 `ratio_threshold`；
3. 仍相同时，选择 solution 字典序最小的方案。

保证结果可复现。

---

## 11. 测试要求

至少覆盖：

- 正常整数成本；
- 小数成本；
- 单个和多个空值；
- 全空行；
- `ratio_threshold = 0 / 0.6 / 接近1`；
- 无可行解；
- 多最优解；
- 大数和数值边界；
- 密态结果与明文基准结果一致。

核心验证：

```text
solution 长度 = n
solution[i] ∈ {1,2,3}
空值方式不会被选择
total_cost 与 solution 一致
ratio >= ratio_threshold
ratio < 1
结果为全局最优
```

---

## 12. 本期范围

支持：

```text
n × 3 成本矩阵
三选一
成本最小化
单 ratio 约束
CSV 输入
空成本
密态计算
C++ / Python / Java 接口
```

暂不支持：

```text
m 动态变化
多 ratio 约束
多目标优化
连续变量
通用 LP/MILP
自定义约束
```