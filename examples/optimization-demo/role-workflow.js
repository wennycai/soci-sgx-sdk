const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)],te=new TextEncoder(),td=new TextDecoder();
const S=1e6,state={plain:null,ownerName:'',ownerPackage:null,bPackage:null,resultPackage:null,computed:null};
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&apos;'}[c]));
function bytes(v){return v instanceof Uint8Array?v:new Uint8Array(v)}function crc32(data){let c=0xffffffff;for(const b of data){c^=b;for(let k=0;k<8;k++)c=(c>>>1)^((c&1)?0xedb88320:0)}return(c^0xffffffff)>>>0}function u16(a,o,v){a[o]=v&255;a[o+1]=v>>>8&255}function u32(a,o,v){u16(a,o,v);u16(a,o+2,v>>>16)}function concat(parts){const n=parts.reduce((s,p)=>s+p.length,0),out=new Uint8Array(n);let o=0;for(const p of parts){out.set(p,o);o+=p.length}return out}
function zip(entries){const local=[],central=[];let offset=0;for(const [name,value] of entries){const n=te.encode(name),d=bytes(value),crc=crc32(d),h=new Uint8Array(30);u32(h,0,0x04034b50);u16(h,4,20);u16(h,6,0x800);u32(h,14,crc);u32(h,18,d.length);u32(h,22,d.length);u16(h,26,n.length);local.push(h,n,d);const c=new Uint8Array(46);u32(c,0,0x02014b50);u16(c,4,20);u16(c,6,20);u16(c,8,0x800);u32(c,16,crc);u32(c,20,d.length);u32(c,24,d.length);u16(c,28,n.length);u32(c,42,offset);central.push(c,n);offset+=h.length+n.length+d.length}const cd=concat(central),end=new Uint8Array(22);u32(end,0,0x06054b50);u16(end,8,entries.length);u16(end,10,entries.length);u32(end,12,cd.length);u32(end,16,offset);return concat([...local,cd,end])}
async function unzip(input){const a=bytes(input),dv=new DataView(a.buffer,a.byteOffset,a.byteLength);let e=-1;for(let i=a.length-22;i>=Math.max(0,a.length-65557);i--)if(dv.getUint32(i,true)===0x06054b50){e=i;break}if(e<0)throw Error('不是有效的 ZIP/XLSX/ENC 文件');const count=dv.getUint16(e+10,true),cd=dv.getUint32(e+16,true),out={};let p=cd;for(let i=0;i<count;i++){if(dv.getUint32(p,true)!==0x02014b50)throw Error('ZIP 目录损坏');const method=dv.getUint16(p+10,true),cs=dv.getUint32(p+20,true),nl=dv.getUint16(p+28,true),xl=dv.getUint16(p+30,true),cl=dv.getUint16(p+32,true),off=dv.getUint32(p+42,true),name=td.decode(a.slice(p+46,p+46+nl)),ln=dv.getUint16(off+26,true),lx=dv.getUint16(off+28,true),raw=a.slice(off+30+ln+lx,off+30+ln+lx+cs);if(method===0)out[name]=raw;else if(method===8){const stream=new Blob([raw]).stream().pipeThrough(new DecompressionStream('deflate-raw'));out[name]=new Uint8Array(await new Response(stream).arrayBuffer())}else throw Error(`不支持 ZIP 压缩方法 ${method}`);p+=46+nl+xl+cl}return out}
function colName(n){let s='';for(n++;n;n=Math.floor((n-1)/26))s=String.fromCharCode(65+(n-1)%26)+s;return s}function xlsx(rows){const sheet=rows.map((row,r)=>`<row r="${r+1}">${row.map((v,c)=>`<c r="${colName(c)}${r+1}" t="inlineStr"><is><t>${esc(v??'')}</t></is></c>`).join('')}</row>`).join('');return zip([['[Content_Types].xml',te.encode(`<?xml version="1.0"?><Types xmlns="http://schemas.openxmlformats.org/package/2006/content-types"><Default Extension="rels" ContentType="application/vnd.openxmlformats-package.relationships+xml"/><Default Extension="xml" ContentType="application/xml"/><Override PartName="/xl/workbook.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet.main+xml"/><Override PartName="/xl/worksheets/sheet1.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.worksheet+xml"/></Types>`)],['_rels/.rels',te.encode(`<?xml version="1.0"?><Relationships xmlns="http://schemas.openxmlformats.org/package/2006/relationships"><Relationship Id="rId1" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/officeDocument" Target="xl/workbook.xml"/></Relationships>`)],['xl/workbook.xml',te.encode(`<?xml version="1.0"?><workbook xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main" xmlns:r="http://schemas.openxmlformats.org/officeDocument/2006/relationships"><sheets><sheet name="EncryptedData" sheetId="1" r:id="rId1"/></sheets></workbook>`)],['xl/_rels/workbook.xml.rels',te.encode(`<?xml version="1.0"?><Relationships xmlns="http://schemas.openxmlformats.org/package/2006/relationships"><Relationship Id="rId1" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/worksheet" Target="worksheets/sheet1.xml"/></Relationships>`)],['xl/worksheets/sheet1.xml',te.encode(`<?xml version="1.0"?><worksheet xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main"><sheetData>${sheet}</sheetData></worksheet>`)]])}
async function readXlsx(buffer){const z=await unzip(buffer),sheet=z['xl/worksheets/sheet1.xml'];if(!sheet)throw Error('Excel 中找不到第一个工作表');let shared=[];if(z['xl/sharedStrings.xml']){const d=new DOMParser().parseFromString(td.decode(z['xl/sharedStrings.xml']),'text/xml');shared=[...d.querySelectorAll('si')].map(x=>[...x.querySelectorAll('t')].map(t=>t.textContent).join(''))}const doc=new DOMParser().parseFromString(td.decode(sheet),'text/xml'),rows=[];for(const row of doc.querySelectorAll('row')){const values=[];for(const c of row.querySelectorAll('c')){const ref=c.getAttribute('r')||'A1',col=[...ref.match(/^[A-Z]+/)[0]].reduce((n,x)=>n*26+x.charCodeAt(0)-64,0)-1,type=c.getAttribute('t'),v=c.querySelector('v')?.textContent??'',inline=[...c.querySelectorAll('is t')].map(x=>x.textContent).join('');values[col]=type==='s'?shared[Number(v)]:(type==='inlineStr'||type==='str'?inline||v:v)}rows.push(values)}return rows}
function parseCosts(rows){const out=[];for(let i=1;i<rows.length;i++){if(!rows[i]||rows[i].every(x=>x==null||x===''))continue;const costs=[1,2,3].map(j=>{const raw=rows[i][j];if(raw==null||String(raw).trim()==='')return null;const text=String(raw).trim();if(!/^\d+(?:\.\d{1,6})?$/.test(text))throw Error(`第 ${i+1} 行成本必须是非负且最多六位小数`);const v=Number(text);if(!Number.isFinite(v))throw Error(`第 ${i+1} 行成本超出有效范围`);return v});if(costs.every(v=>v==null))throw Error(`第 ${i+1} 行没有可用方式`);out.push({id:String(rows[i][0]||`M${i}`),costs})}if(!out.length)throw Error('Excel 没有有效数据');return out}
function wire(items,threshold=0){return `${threshold}\n`+items.map(x=>[x.id,...x.costs.map(v=>v==null?'':v)].join('\t')).join('\n')}async function api(path,body){const r=await fetch(path,{method:'POST',headers:{'Content-Type':'text/plain;charset=utf-8'},body}),d=await r.json();if(!r.ok)throw Error(d.error||'SOCI 后端调用失败');return d}
function download(data,name,type='application/octet-stream'){const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([data],{type}));a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}
function makeEnc(manifest,rows){return zip([['encrypted-data.xlsx',xlsx(rows)],['manifest.json',te.encode(JSON.stringify(manifest,null,2))]])}async function readEnc(file){const z=await unzip(await file.arrayBuffer());if(!z['manifest.json']||!z['encrypted-data.xlsx'])throw Error('加密包结构不完整');const m=JSON.parse(td.decode(z['manifest.json']));if(m.format!=='SOCI-ROLE-ENC'||m.version!==1)throw Error('不支持的加密包');return m}
function wireDrop(zone,input,handler){zone.onclick=e=>{if(e.target.tagName!=='BUTTON')input.click()};zone.querySelector('button').onclick=e=>{e.stopPropagation();input.click()};zone.ondragover=e=>{e.preventDefault();zone.classList.add('drag')};zone.ondragleave=()=>zone.classList.remove('drag');zone.ondrop=e=>{e.preventDefault();zone.classList.remove('drag');if(e.dataTransfer.files[0])handler(e.dataTransfer.files[0]).catch(showError)};input.onchange=()=>input.files[0]&&handler(input.files[0]).catch(showError)}function showError(e){alert(e.message);console.error(e)}

$$('[data-role]').forEach(b=>b.onclick=()=>{$$('[data-role]').forEach(x=>x.classList.toggle('active',x===b));$('#ownerPanel').classList.toggle('hidden',b.dataset.role!=='owner');$('#computePanel').classList.toggle('hidden',b.dataset.role!=='compute')});
$$('[name=ownerInput]').forEach(r=>r.onchange=()=>{$('#ownerPlainCard').classList.toggle('hidden',r.value!=='plain'||!r.checked);$('#ownerEncryptedCard').classList.toggle('hidden',r.value!=='encrypted'||!r.checked)});
wireDrop($('#ownerPlainDrop'),$('#ownerPlainFile'),async file=>{if(!file.name.toLowerCase().endsWith('.xlsx'))throw Error('A 的明文输入必须是 .xlsx');state.plain=parseCosts(await readXlsx(await file.arrayBuffer()));state.ownerName=file.name;$('#ownerPlainMeta').classList.remove('hidden');$('#ownerPlainMeta').innerHTML=`<span>✓ ${esc(file.name)} · ${state.plain.length} 行</span><b>明文仅 A 可见</b>`;$('#ownerEncrypt').disabled=false});
$('#ownerEncrypt').onclick=async()=>{try{const d=await api('/api/workflow/encrypt',wire(state.plain)),manifest={format:'SOCI-ROLE-ENC',version:1,stage:'encrypted-input',job_id:d.jobId,row_count:state.plain.length,algorithm:'Paillier-3072',created_by:'role-A',created_at:new Date().toISOString()},rows=[['MATERIAL_ID','ENC_METHOD_1','ENC_METHOD_2','ENC_METHOD_3'],...state.plain.map((x,i)=>[btoa(unescape(encodeURIComponent(x.id))),...d.rows[i]])];state.ownerPackage=makeEnc(manifest,rows);download(state.ownerPackage,'soci-input-for-B.enc');$('#ownerPlainCard .stage-state').textContent='已加密并导出'}catch(e){showError(e)}};
wireDrop($('#computeDrop'),$('#computeFile'),async file=>{const m=await readEnc(file);if(m.stage!=='encrypted-input')throw Error('请选择角色 A 生成的待计算加密文件');state.bPackage=m;$('#computeMeta').classList.remove('hidden');$('#computeMeta').textContent=`✓ 加密文件校验通过 · ${m.row_count} 行数据`;$('#computeOptimize').disabled=false});
$('#computeOptimize').onclick=async()=>{const button=$('#computeOptimize');try{button.disabled=true;button.firstChild.textContent='正在计算 ';const t=Number($('#computeThreshold').value);if(!(t>=0&&t<1))throw Error('阈值必须满足 0 ≤ T < 1');const d=await api('/api/workflow/compute',`${state.bPackage.job_id}\t${t}`),manifest={...state.bPackage,stage:'computed-result',computed_by:'role-B',threshold:t,encrypted_result:d.ciphertexts,computed_at:new Date().toISOString()};state.computed=manifest;state.resultPackage=makeEnc(manifest,[['RESULT_COLUMN'],...d.ciphertexts.map(x=>[x])]);$('#computeResult').classList.remove('hidden');$('#computeResult code').textContent=d.ciphertexts[0];$('#computeExport').classList.remove('hidden')}catch(e){showError(e)}finally{button.disabled=false;button.firstChild.textContent='开始优化计算 '}};
$('#computeExport').onclick=()=>download(state.resultPackage,'soci-computed-for-A.enc');
wireDrop($('#ownerResultDrop'),$('#ownerResultFile'),async file=>{const m=await readEnc(file);if(m.stage!=='computed-result'||!Array.isArray(m.encrypted_result))throw Error('A 的密文输入必须是 B 返回的计算结果文件');state.computed=m;$('#ownerResultMeta').classList.remove('hidden');$('#ownerResultMeta').textContent=`✓ B 的计算结果 · 阈值 ${m.threshold} · 仅最后一列可解密`;$('#ownerDecrypt').disabled=false});
$('#ownerDecrypt').onclick=async()=>{try{const d=await api('/api/workflow/decrypt',state.computed.encrypted_result.join('\n'));$('#ownerDecoded').classList.remove('hidden');$('#ownerDecoded').innerHTML=`<div class="decrypt-summary"><div><span>最优总成本</span><b>${d.totalCost}</b></div><div><span>最终 ratio</span><b>${(d.ratio*100).toFixed(4)}%</b></div><div><span>状态</span><b>OPTIMAL</b></div></div><div class="decrypt-solution">solution = [${d.solution.join(', ')}]</div>`;download(te.encode(JSON.stringify({format:'SOCI-DECRYPTED-RESULT',from:'role-A',to:'role-B',...d},null,2)),'soci-result-for-B.json','application/json');$('#ownerEncryptedCard .stage-state').textContent='最后一列已解密并返还 B'}catch(e){showError(e)}};
